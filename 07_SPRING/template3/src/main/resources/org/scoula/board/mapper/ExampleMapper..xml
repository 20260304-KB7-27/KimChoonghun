<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace는 반드시 경로 + 인터페이스 패키지명과 일치해야한다. -->
<mapper namespace="org.scoula.board.mapper.ExampleMapper">
    <select id="selectAllByMapper" resultType="org.scoula.domain.BoardVO">
        <!-- CDATA 섹션 : 안에 있는 내용은 하나의 문자열로 취급하겠다. -->
        <![CDATA[
        select * from tbl_board_ex
        ]]>
    </select>
</mapper>

<!--<mapper namespace="연결하고 싶은 인터페이스 경로 + 이름">-->
<!--    <select id="추상 메서드 명" resultType="반환타입">-->

<!--    </select>-->

<!--</mapper>-->